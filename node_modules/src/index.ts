//interfaces ( type x interface)

type robot = { // tipar
    readonly  id:number | string;
    name: string;
};

const bot: robot = {
    id:1,
    name: "Wally",
};

interface robot2{ // trabalhar com classes
   readonly id: number | string;
    name: string;
};