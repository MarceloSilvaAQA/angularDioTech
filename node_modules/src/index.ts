/**
 *  variaveis
 */
//tipos Primitivos : boolen, number, string

let ligado: boolean = false;
let nome: string = "marcelo";
let idade: number = 30;
let altura: number = 1.65;

//Tipos especiais: null, undefined
let nulo : null = null;
let indefinino : undefined = undefined;

//tipos abrangentes: any, void

let retorno:void
let retornoView:any = "Qualquer Coisa";

//objeto - sem previsibilidade
let produto: object = {
    name:"Marcelo",
    cidade: "Sp",
    idade: 22,
};
//objeto tipado - com previsibilidade
type ProdutoLoja = {
    nome: string;
    preco: number;
    unidades: number;
};

let meuProduto: ProdutoLoja = {
    nome: "Tenis",
    preco: 89.90,
    unidades: 5,
}

/**
 *  arrays
 */

let dados: string[] = ["felipe", "marcelo", "ana"];
let dados2: Array<string> = ["felipe", "marcelo", "ana"];

/**
 *  multi Types
 */

let infos:(string | number)[] = ["Marcelo", 22, "Ana", "Adriana"];

/**
 * Tuplas
 */
let boleto: [string, number, number] = ["Agua conta", 199.90, 32325456];

/**
 * Dates
 */
let aniversario: Date = new Date("2023-05-21 05:00");
console.log(aniversario.toString());

